import React from "react";
import {Link} from 'react-router-dom'

export default function Roles() {
  return (
    <div>
    <h1>Roles</h1>

    <button>PM</button>
    <button>User</button>

    </div>
  )
}